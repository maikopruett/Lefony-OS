diff --git a/hw/arm/fsl-imx6ul.c b/hw/arm/fsl-imx6ul.c
--- a/hw/arm/fsl-imx6ul.c
+++ b/hw/arm/fsl-imx6ul.c
@@ -604,6 +604,10 @@ static void fsl_imx6ul_realize(DeviceState *dev, Error **errp)
      */
     sysbus_realize(SYS_BUS_DEVICE(&s->snvs), &error_abort);
     sysbus_mmio_map(SYS_BUS_DEVICE(&s->snvs), 0, FSL_IMX6UL_SNVS_HP_ADDR);
+    /* Route SNVS through its architectural GIC input so the guest can
+     * acknowledge and clear the source after an alarm or power event. */
+    sysbus_connect_irq(SYS_BUS_DEVICE(&s->snvs), 0,
+                       qdev_get_gpio_in(gic, FSL_IMX6UL_SNVS_IRQ));
 
     /*
      * Watchdogs
diff --git a/hw/misc/imx7_snvs.c b/hw/misc/imx7_snvs.c
--- a/hw/misc/imx7_snvs.c
+++ b/hw/misc/imx7_snvs.c
@@ -15,6 +15,7 @@
 #include "qemu/bitops.h"
 #include "qemu/timer.h"
 #include "migration/vmstate.h"
+#include "hw/core/irq.h"
 #include "hw/misc/imx7_snvs.h"
 #include "qemu/cutils.h"
 #include "qemu/module.h"
@@ -28,13 +29,14 @@
 
 static const VMStateDescription vmstate_imx7_snvs = {
     .name = TYPE_IMX7_SNVS,
-    .version_id = 2,
+    .version_id = 3,
     .minimum_version_id = 1,
     .fields = (const VMStateField[]) {
         VMSTATE_UINT64(tick_offset, IMX7SNVSState),
         VMSTATE_UINT64(lpcr, IMX7SNVSState),
         VMSTATE_UINT32_V(hpcomr, IMX7SNVSState, 2),
         VMSTATE_UINT32_V(hpcr, IMX7SNVSState, 2),
+        VMSTATE_UINT32_V(hpsr, IMX7SNVSState, 3),
         VMSTATE_END_OF_LIST()
     }
 };
@@ -58,8 +60,7 @@ static uint64_t imx7_snvs_read(void *opaque, hwaddr offset, unsigned size)
         ret = s->hpcr;
         break;
     case SNVS_HPSR:
-        /* No power-button or security event is asserted by default. */
-        ret = 0;
+        ret = s->hpsr;
         break;
     case SNVS_LPSRTCMR:
         ret = extract64(imx7_snvs_get_count(s), 32, 15);
@@ -85,6 +86,8 @@ static void imx7_snvs_reset(DeviceState *dev)
     s->lpcr = 0;
     s->hpcomr = 0;
     s->hpcr = 0;
+    s->hpsr = 0;
+    qemu_irq_lower(s->irq);
 }
 
 static void imx7_snvs_write(void *opaque, hwaddr offset,
@@ -105,6 +108,11 @@ static void imx7_snvs_write(void *opaque, hwaddr offset,
     case SNVS_HPCR:
         s->hpcr = v;
         break;
+    case SNVS_DIAG_PWRKEY:
+        /* Emulator-only power-key ingress used by qtest and the UI proxy. */
+        s->hpsr = v ? BIT(6) : 0;
+        qemu_set_irq(s->irq, v != 0);
+        break;
     case SNVS_LPSRTCMR:
         new_value = deposit64(snvs_count, 32, 32, v);
         break;
@@ -153,6 +161,7 @@ static void imx7_snvs_init(Object *obj)
                           TYPE_IMX7_SNVS, 0x1000);
 
     sysbus_init_mmio(sd, &s->mmio);
+    sysbus_init_irq(sd, &s->irq);
 
     qemu_get_timedate(&tm, 0);
     s->tick_offset = mktimegm(&tm) -
diff --git a/include/hw/misc/imx7_snvs.h b/include/hw/misc/imx7_snvs.h
--- a/include/hw/misc/imx7_snvs.h
+++ b/include/hw/misc/imx7_snvs.h
@@ -21,6 +21,7 @@ enum IMX7SNVSRegisters {
     SNVS_HPCOMR = 0x04,
     SNVS_HPCR = 0x08,
     SNVS_HPSR = 0x14,
+    SNVS_DIAG_PWRKEY = 0xfc,
     SNVS_LPCR = 0x38,
     SNVS_LPCR_TOP   = BIT(6),
     SNVS_LPCR_DP_EN = BIT(5),
@@ -41,6 +42,8 @@ struct IMX7SNVSState {
     uint64_t lpcr;
     uint32_t hpcomr;
     uint32_t hpcr;
+    uint32_t hpsr;
+    qemu_irq irq;
 };
 
 #endif /* IMX7_SNVS_H */
